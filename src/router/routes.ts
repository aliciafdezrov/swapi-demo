interface SwitchRoutes {
    root: string;
    starships: string;
    planets: string;
}

export const switchRoutes: SwitchRoutes = {
    root: '/',
    starships: '/starships',
    planets: '/planets',
};
