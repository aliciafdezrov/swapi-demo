import React from 'react';
import {MainLayout} from "../layouts/main.layout";
import {PlanetsContainer} from "../pods/planets";

export const PlanetsScene: React.FC = () => {
    return (
        <PlanetsContainer/>
    );
};
